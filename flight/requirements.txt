# Flight segment dependencies
# Python 3.11+ required. Usage: pip install -r requirements.txt

# --- Core FSW ---
sdnotify==0.3.2          # Systemd watchdog notifications (sd_notify)

# --- Communications ---
websockets==12.0         # Async WebSocket client

# --- Cryptography ---
cryptography>=42.0.0     # Ed25519 signatures (OpenSSL)
PyNaCl==1.6.2            # libsodium bindings for crypto_verifier (implements RFC 8032)

# --- Testing ---
pytest==8.3.4            # Unit test runner
pytest-asyncio==0.24.0   # Async test support for websockets